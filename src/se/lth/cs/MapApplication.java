package se.lth.cs;

import java.util.Map;

public class MapApplication  extends Application<Map<Integer, Integer>> {

    MapApplication(int seed, String configuration, Map<Integer, Integer> structure) {
        super(seed, configuration, structure);
    }
}
